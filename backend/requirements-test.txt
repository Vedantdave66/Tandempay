# Test-only dependencies — not needed for production deployment.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-test.txt
#
# SQLite-backed tests (no Postgres needed in CI):
#   test_financial_precision.py
#   test_idempotency_concurrency.py
#   test_payment_concurrency.py
#
# PostgreSQL-required tests (skipped automatically without a live PG connection):
#   test_pg_concurrency.py
#   test_pg_failure_injection.py
#   test_settlement_constraints.py  ← self-skips when DATABASE_URL != postgresql://
#   test_stripe_idempotency.py      ← check individually

pytest>=8.2.0
pytest-asyncio>=0.23.0
anyio[trio]>=4.4.0
httpx>=0.27.0
aiosqlite>=0.20.0
