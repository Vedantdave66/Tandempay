services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tandempay
      POSTGRES_PASSWORD: tandempay
      POSTGRES_DB: tandempay
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
